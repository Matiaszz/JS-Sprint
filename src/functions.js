function sayHi(name) {
  console.log(`sup ${name}!`);
}
sayHi("matias"); // -> sup matias!

function multiply(n1, n2) {
  const result = n1 * n2;
  return result;
}

console.log(multiply(3, 2)); // -> 6
