let productColors = ["blue", "white", "black"];

printf = console.log;
printf(productColors); // prints all items of the array
printf(productColors[1]); // referer by index to access the value and i can modify the value too
printf(productColors.length); // its len() from python
